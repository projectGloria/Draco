node_modules/
out/
dist/
release/
*.tsbuildinfo
# The extension's private key pins its ID. It is generated once, locally, and
# must never be committed - anyone holding it can impersonate the extension.
extension/key.pem
host/draco-host.exe
